import { Box } from "@chakra-ui/react";
import React from "react";

interface IFilterBar {}
const FilterBar: React.FC<IFilterBar> = () => {
  return <Box padding={"0.5rem"} bg={"white"}>FilterBar</Box>;
};

export default FilterBar;
